let c=10

let d=25
let e=85

// to export anything in js here like 'c' in one.js then we can use

//module.exports = c  // here module is an object and export is the property now everything in c will be exported


// If we want to export more then one thing then we can create an object of it

module.exports={one:c,
  two:d,
  three:e,
}